do local Library=loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))();local Window=Library.CreateLib("AKIHUB","DarkTheme");local Tab=Window:NewTab("Info");local Section=Tab:NewSection("สามารถสนับสนุนสคริปได้ที่ดิสคอส");Section:NewLabel("https://discord.gg/NeGv59fYEd");Section:NewLabel("Enjoy KUb :) ");local Tab=Window:NewTab("Main");local Section=Tab:NewSection("Fram");Section:NewButton("Fram Monther","ButtonInfo",function() if _G.AutoFollowRunning then _G.AutoFollowRunning=false;warn("⛔ หยุดการทำงานของสคริปต์ติดตามมอนสเตอร์แล้ว");return;else _G.AutoFollowRunning=true;warn("✅ เริ่มทำงานสคริปต์ติดตามมอนสเตอร์");end local player=game.Players.LocalPlayer;local character=player.Character or player.CharacterAdded:Wait() ;local hrp=character:WaitForChild("HumanoidRootPart");local aiFolder=workspace:WaitForChild("AI");player.CharacterAdded:Connect(function(newChar) character=newChar;task.wait(1);hrp=character:WaitForChild("HumanoidRootPart");warn("ัวละครเกิดใหม่แล้ว อัปเดต HumanoidRootPart สำเร็จ");end);local monsterOrder={"Bulk Ghoul","Athlete","Human","Investigator","Rin Ghoul","Serpent Ghoul"};local currentIndex=1;local function findAliveMonsters(monsterName) local alive={};for _,monster in pairs(aiFolder:GetChildren()) do if ((monster.Name==monsterName) and monster:FindFirstChild("Humanoid") and (monster.Humanoid.Health>0)) then table.insert(alive,monster);end end return alive;end local function followMonster(monster) local humanoid=monster:FindFirstChild("Humanoid");local monsterHRP=monster:FindFirstChild("HumanoidRootPart");if ( not humanoid or  not monsterHRP) then return;end print("🎯 กำลังตาม:",monster.Name);while _G.AutoFollowRunning and (humanoid.Health>0) and monsterHRP and hrp  do task.wait(0.1);if ( not hrp or  not hrp.Parent) then repeat task.wait(0.5);until character and character:FindFirstChild("HumanoidRootPart")  hrp=character:FindFirstChild("HumanoidRootPart");print("🧍 อัปเดต HRP หลังจากตาย");end local distance=(hrp.Position-monsterHRP.Position).Magnitude;if (distance>100) then hrp.CFrame=monsterHRP.CFrame + Vector3.new(0,0, -5) ;print("⚡ วาร์ปเข้าใกล้:",monster.Name);else local followPos=monsterHRP.CFrame * CFrame.new(0,2.5,5) ;hrp.CFrame=followPos;end end print("💀",monster.Name,"ตายแล้ว!");end task.spawn(function() while _G.AutoFollowRunning do task.wait(0.1);local currentMonsterName=monsterOrder[currentIndex];print("🔍 ค้นหามอน:",currentMonsterName);local aliveMonsters=findAliveMonsters(currentMonsterName);if ( #aliveMonsters>0) then for _,monster in ipairs(aliveMonsters) do if  not _G.AutoFollowRunning then break;end followMonster(monster);end else currentIndex=currentIndex + 1 ;if (currentIndex> #monsterOrder) then currentIndex=1;end print("➡️ เปลี่ยนเป้าหมายไปที่:",monsterOrder[currentIndex]);end end warn("🛑 สคริปต์หยุดทำงานเรียบร้อยแล้ว");end);end);Section:NewButton("Fast Attack","ButtonInfo",function() local ReplicatedStorage=game:GetService("ReplicatedStorage");local remote=ReplicatedStorage:WaitForChild("BridgeNet2"):WaitForChild("dataRemoteEvent");while true do local args={{"NormalAttack","\003"}};remote:FireServer(unpack(args));task.wait(0.001);end end);Section:NewButton("Auto Use Waepon","use waepon auto",function() local player=game.Players.LocalPlayer;local function pressE() local VirtualInputManager=game:GetService("VirtualInputManager");task.wait(1);VirtualInputManager:SendKeyEvent(true,Enum.KeyCode.E,false,game);task.wait(0.1);VirtualInputManager:SendKeyEvent(false,Enum.KeyCode.E,false,game);end pressE();player.CharacterAdded:Connect(function() pressE();end);end);local Tab=Window:NewTab("Boss");local Section=Tab:NewSection("Boss List");Section:NewButton("Auto Kaneki","ButtonInfo",function() if (_G.KanekiFollowActive==nil) then _G.KanekiFollowActive=false;end if _G.KanekiFollowActive then _G.KanekiFollowActive=false;print("❌ หยุดการลอยตาม Kaneki แล้ว");if _G.KanekiFollowConnection then _G.KanekiFollowConnection:Disconnect();_G.KanekiFollowConnection=nil;end return;end _G.KanekiFollowActive=true;print("✅ เริ่มลอยตามมอนเตอร์ Kaneki แล้ว...");local Players=game:GetService("Players");local RunService=game:GetService("RunService");local player=Players.LocalPlayer;local char=player.Character or player.CharacterAdded:Wait() ;local function getHRP(model) if  not model then return nil;end return model:FindFirstChild("HumanoidRootPart") or model:FindFirstChild("Torso") or model:FindFirstChild("UpperTorso") ;end local aiFolder=workspace:WaitForChild("AI",5);local targetName="Kaneki";local followDistance=3;local floatHeight=2.5;local smoothing=0.25;local function findTarget() if  not aiFolder then return nil;end for _,v in ipairs(aiFolder:GetChildren()) do if (v.Name==targetName) then return v;end end return nil;end local hrp=getHRP(char);if  not hrp then char:WaitForChild("HumanoidRootPart",10);hrp=getHRP(char);end local target=findTarget();if  not target then warn("ไม่พบมอนเตอร์ชื่อ '"   .. targetName   .. "' ใน workspace.AI" );_G.KanekiFollowActive=false;return;end local targetHRP=getHRP(target);if  not targetHRP then warn("มอนเตอร์ '"   .. targetName   .. "' ไม่มี HumanoidRootPart" );_G.KanekiFollowActive=false;return;end _G.KanekiFollowConnection=RunService.Heartbeat:Connect(function(dt) if  not _G.KanekiFollowActive then return;end if ( not target or  not target.Parent) then target=findTarget();if target then targetHRP=getHRP(target);else return;end end if ( not targetHRP or  not targetHRP.Parent) then targetHRP=getHRP(target);if  not targetHRP then return;end end local behindPos=targetHRP.Position-(targetHRP.CFrame.LookVector * followDistance) ;behindPos=behindPos + Vector3.new(0,floatHeight,0) ;local desiredCFrame=CFrame.new(behindPos,targetHRP.Position);local newCFrame=hrp.CFrame:lerp(desiredCFrame,math.clamp(smoothing,0,1));hrp.CFrame=newCFrame;end);end);Section:NewButton("AutoJson","ButtonInfo",function() if (_G.JsonFollowActive==nil) then _G.JsonFollowActive=false;end if _G.JsonFollowActive then _G.JsonFollowActive=false;print("❌ หยุดการลอยตาม Json แล้ว");if _G.JsonFollowConnection then _G.JsonFollowConnection:Disconnect();_G.JsonFollowConnection=nil;end return;end _G.JsonFollowActive=true;print("✅ เริ่มลอยตามมอนสเตอร์ Json แล้ว...");local Players=game:GetService("Players");local RunService=game:GetService("RunService");local player=Players.LocalPlayer;local char=player.Character or player.CharacterAdded:Wait() ;local function getHRP(model) if  not model then return nil;end return model:FindFirstChild("HumanoidRootPart") or model:FindFirstChild("Torso") or model:FindFirstChild("UpperTorso") ;end local aiFolder=workspace:WaitForChild("AI",5);local targetName="Json";local followDistance=3;local floatHeight=2.5;local smoothing=0.25;local function findTarget() if  not aiFolder then return nil;end for _,v in ipairs(aiFolder:GetChildren()) do if (v.Name==targetName) then return v;end end return nil;end local hrp=getHRP(char);if  not hrp then char:WaitForChild("HumanoidRootPart",10);hrp=getHRP(char);end local target=findTarget();if  not target then warn("ไม่พบมอนสเตอร์ชื่อ '"   .. targetName   .. "' ใน workspace.AI" );_G.JsonFollowActive=false;return;end local targetHRP=getHRP(target);if  not targetHRP then warn("มอนสเตอร์ '"   .. targetName   .. "' ไม่มี HumanoidRootPart" );_G.JsonFollowActive=false;return;end _G.JsonFollowConnection=RunService.Heartbeat:Connect(function(dt) if  not _G.JsonFollowActive then return;end if ( not target or  not target.Parent) then target=findTarget();if target then targetHRP=getHRP(target);else return;end end if ( not targetHRP or  not targetHRP.Parent) then targetHRP=getHRP(target);if  not targetHRP then return;end end local behindPos=targetHRP.Position-(targetHRP.CFrame.LookVector * followDistance) ;behindPos=behindPos + Vector3.new(0,floatHeight,0) ;local desiredCFrame=CFrame.new(behindPos,targetHRP.Position);local newCFrame=hrp.CFrame:lerp(desiredCFrame,math.clamp(smoothing,0,1));hrp.CFrame=newCFrame;end);end);local Tab=Window:NewTab("Shop");local Section=Tab:NewSection("CCG WAEPON");Section:NewButton("Fueguchi 750K","ButtonInfo",function() local args={"Fueguchi"};game:GetService("ReplicatedStorage"):WaitForChild("Network"):WaitForChild("{5776F50B-7085-4389-8CC6-B21ED9601425}"):InvokeServer(unpack(args));end);Section:NewButton("Doujima 1.2M","ButtonInfo",function() local args={"Doujima"};game:GetService("ReplicatedStorage"):WaitForChild("Network"):WaitForChild("{5776F50B-7085-4389-8CC6-B21ED9601425}"):InvokeServer(unpack(args));end);Section:NewButton("Kura 3m","ButtonInfo",function() local args={"Kura"};game:GetService("ReplicatedStorage"):WaitForChild("Network"):WaitForChild("{5776F50B-7085-4389-8CC6-B21ED9601425}"):InvokeServer(unpack(args));end);local Tab=Window:NewTab("Teleport");local Section=Tab:NewSection("Save Zone");Section:NewButton("CCG","ButtonInfo",function() local player=game.Players.LocalPlayer;local character=player.Character or player.CharacterAdded:Wait() ;local hrp=character:WaitForChild("HumanoidRootPart");local targetPosition=Vector3.new( -602,36, -337);local function teleport() hrp.CFrame=CFrame.new(targetPosition);end teleport();end);Section:NewButton("Ghoul","ButtonInfo",function() local player=game.Players.LocalPlayer;local character=player.Character or player.CharacterAdded:Wait() ;local hrp=character:WaitForChild("HumanoidRootPart");local targetPosition=Vector3.new(184,35,400);local function teleport() hrp.CFrame=CFrame.new(targetPosition);end teleport();end);Section:NewButton("Cafe","ButtonInfo",function() local player=game.Players.LocalPlayer;local character=player.Character or player.CharacterAdded:Wait() ;local hrp=character:WaitForChild("HumanoidRootPart");local targetPosition=Vector3.new(17,50, -262);local function teleport() hrp.CFrame=CFrame.new(targetPosition);end teleport();end);local Section=Tab:NewSection("Boss");Section:NewButton("Json","ButtonInfo",function() local player=game.Players.LocalPlayer;local character=player.Character or player.CharacterAdded:Wait() ;local hrp=character:WaitForChild("HumanoidRootPart");local targetPosition=Vector3.new( -264,35,410);local function teleport() hrp.CFrame=CFrame.new(targetPosition);end teleport();end);Section:NewButton("Kaneki","ButtonInfo",function() local player=game.Players.LocalPlayer;local character=player.Character or player.CharacterAdded:Wait() ;local hrp=character:WaitForChild("HumanoidRootPart");local targetPosition=Vector3.new(478,35, -7);local function teleport() hrp.CFrame=CFrame.new(targetPosition);end teleport();end);local Tab=Window:NewTab("Setting");local Section=Tab:NewSection("Setting");Section:NewSlider("SpeedWalk","SliderInfo",500,0,function(s) game.Players.LocalPlayer.Character.Humanoid.WalkSpeed=s;end);Section:NewButton("Rejoin","ButtonInfo",function() local Players=game:GetService("Players");local TeleportService=game:GetService("TeleportService");local player=Players.LocalPlayer;local placeId=game.PlaceId;local function rejoinGame() TeleportService:Teleport(placeId,player);end rejoinGame();end);Section:NewKeybind("KeybindHideUI","KeybindInfo",Enum.KeyCode.F,function() Library:ToggleUI();end); end
